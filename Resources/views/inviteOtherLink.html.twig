{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set options = {
    class: cssClass is defined ? cssClass : '',
    aCss: 'no-hash',
    iCss: 'icon-other',
    label: 'oro.othercall.call.other_call'|trans,
    pageComponent: {
        module: 'oroothercall/js/app/components/invite-other-component',
        options: {
            modalOptions: {
                otherOptions: otherOptions is defined ? otherOptions : {}
            }
        }|merge(extraComponentOptions is defined ? extraComponentOptions : {})
    }
} %}

{% block action_controll %}
    {{ UI.clientLink(options) }}
{% endblock %}
