require({
    config: {
        'oroothercall/js/app/views/start-button-view': {
            initialApps: {{ get_othercall_initail_apps()|json_encode|raw }},
            appHost: {{ app.request.host|json_encode|raw }}
        }
    }
});
